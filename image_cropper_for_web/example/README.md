# image_cropper_gdx_plus Web example

Demonstrates the Web implementation of `image_cropper_gdx_plus`.

## Getting Started

From this directory, fetch dependencies and run the app in Chrome:

```sh
flutter pub get
flutter run -d chrome
```

See the [main package README](../../image_cropper/README.md) for the required Cropper.js setup and API usage.
