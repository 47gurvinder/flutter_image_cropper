name: image_cropper_gdx_plus_web_example
resolution: workspace
description: Demonstrates the Web implementation of image_cropper_gdx_plus.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.6.0 <4.0.0'
  flutter: '>=3.28.0'

dependencies:
  flutter:
    sdk: flutter

  dotted_border: ^2.1.0
  image_picker: ^1.1.2
  image_picker_for_web: ^3.0.4
  image_cropper_gdx_plus: ^1.0.0

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

# The following section is specific to Flutter.
flutter:

  uses-material-design: true
